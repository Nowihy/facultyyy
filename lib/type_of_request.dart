import 'package:flutter/material.dart';

class TypeOfRequest extends StatelessWidget{
  static const String FacultyName = 'type';
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(

    );
  }
}